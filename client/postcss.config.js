export default {
    plugins: {
        '@tailwindcss/postcss': {},
        autoprefixer: {}, // Autoprefixer is still usually good to have, though v4 might bundle it or not need it as much. Config usually includes it.
    },
}
